import 'package:dartz/dartz.dart';
import '../../../../core/errors/failures.dart';
import '../../../{{userSnake}}/domain/entities/{{userSnake}}_entity.dart';

abstract class AuthRepository {
  Future<Either<Failure, {{className}}Entity>> login(String email, String password);
  Future<Either<Failure, {{className}}Entity>> register(String email, String password, Map<String, dynamic> extra);
  Future<Either<Failure, void>> logout();
  Future<bool> checkAuthStatus();
}
